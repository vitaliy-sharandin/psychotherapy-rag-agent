apiVersion: v1
kind: Secret
metadata:
  name: my-secret
type: Opaque
stringData:
  TAVILY_API_KEY: "${TAVILY_API_KEY}"
  LANGCHAIN_TRACING_V2: "${LANGCHAIN_TRACING_V2}"
  LANGCHAIN_API_KEY: "${LANGCHAIN_API_KEY}"
  INSTRUCTION_MODEL_NAME: "${INSTRUCTION_MODEL_NAME}"
  TEXT_GENERATION_MODEL_NAME: "${TEXT_GENERATION_MODEL_NAME}"
  LLM_ADDRESS: "${LLM_ADDRESS}"
  EMBEDDING_MODEL_ADDRESS: "${EMBEDDING_MODEL_ADDRESS}"